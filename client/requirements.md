## Packages
date-fns | Formatting dates for service logs and reminders
recharts | Visualizing mileage and service cost trends
framer-motion | Smooth transitions and entry animations
lucide-react | Icons for UI elements (already in base but ensuring availability)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Authentication uses Replit Auth.
API routes are defined in @shared/routes.
